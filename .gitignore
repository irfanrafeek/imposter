# macOS
.DS_Store

# Node
node_modules/

# Capacitor / native build artifacts
android/app/build/
android/build/
android/.gradle/
android/captures/
android/local.properties
android/app/release/
ios/App/Pods/
ios/App/build/
DerivedData/

# Firebase
.firebase/
# Emulator output. `firebase emulators:start` drops these in the repo root on
# every run; the rules they are testing are the thing worth keeping (#267).
*-debug.log
